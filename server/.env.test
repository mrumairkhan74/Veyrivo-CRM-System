# Test Environment Configuration
NODE_ENV=test
PORT=5001

# Test Supabase Configuration (Use a separate test project or mock)
SUPABASE_URL=https://test-project.supabase.co
SUPABASE_ANON_KEY=test_anon_key
SUPABASE_SERVICE_ROLE_KEY=test_service_role_key

# JWT Configuration
JWT_SECRET=test-secret-key-for-testing-only-32-chars
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=1d

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Test Database (Optional - use separate test Supabase project)
TEST_DB_URL=https://test-project.supabase.co